<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greenchain.mapper.UserAddressMapper">
    <select id="findByUserId" resultType="com.greenchain.entity.UserAddress">
        SELECT * FROM user_address WHERE user_id = #{userId} ORDER BY is_default DESC, create_time DESC
    </select>
    <select id="findDefaultAddress" resultType="com.greenchain.entity.UserAddress">
        SELECT * FROM user_address WHERE user_id = #{userId} AND is_default = 1 LIMIT 1
    </select>
</mapper>